// Manual :(

function makeAjaxRequestManual (url, method) {
	if (!method) {
		method = 'GET';
	}
	//logic to make the request
}

// Set a default :)
// Remember: does not work if the argument is 'null'
function makeAjaxRequestManual (url, method = 'GET') {
	//logic to make the request
}